# HireFlow 运营后台

Vue 3 + Vite 的 HireFlow 招聘协同运营工作台，连接同招聘渠道中的 Go Gin API。页面覆盖经营总览、人才库、候选人导入、面试流程、招聘提醒和招聘动态。

```bash
npm install
npm test
npm run dev
```

未配置 `VITE_API_BASE_URL` 时自动进入离线演示，演示数据为虚构内容。配置后端地址并登录即可请求 `/api/v1`。


